# Fresh Ready Foods

The legacy demo UI remains available for reference. Production runs the PostgreSQL-backed `production_app.py` service through Docker. See [DEPLOYMENT.md](DEPLOYMENT.md) for setup, M-Pesa configuration, release checks, and rollback.
"# foo-application" 
